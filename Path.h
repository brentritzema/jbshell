#include <iostream>
#include <vector>
#include <stdio.h>
#include <stdlib.h>
#include <string>

using namespace std;

class Path {
    private:
        vector<string> mPath;
    public:
        Path();
        //int find(const string& program) const;
        //string getDirectory(int i) const;
};